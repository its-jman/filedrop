{
	"name": "worker",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"prepare": "npm run gen-types",
		"gen-types": "wrangler types --config ../wrangler.json",
		"typecheck": "npm run gen-types && tsc",
		"dev": "npm run gen-types && wrangler dev --port 3000 --config ../wrangler.json",
		"test": "vitest run",
		"build": "wrangler build"
	}
}
