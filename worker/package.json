{
	"name": "digester-worker",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"prepare": "nr gen-types",
		"gen-types": "wrangler types",
		"typecheck": "nr gen-types && tsc",
		"dev": "nr gen-types && wrangler dev",
		"test": "vitest run"
	}
}
