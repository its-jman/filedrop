{
	"name": "worker",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"prepare": "npm run gen-types",
		"gen-types": "wrangler types",
		"typecheck": "npm run gen-types && tsc",
		"dev": "npm run gen-types && wrangler dev --port 3000",
		"test": "vitest run",
		"build": "wrangler build"
	}
}
